#!/usr/bin/env node

var program = require('commander');

program
  .version('0.0.0')
  .command('createuser [name] [phone]', 'Create a user in the database')
  .command('send-sms', 'Send SMS')
  .parse(process.argv);
